# AUDIT & MAP — Fragmentation Inventory (Phase 1, read-only)

> Scan: 533 files, engine 2.2M, archive 88 case studies, reference/unfairmarket, calendar, media 2,736 dirs. No code modified.

## 1. Redundant Files (keep newest, archive rest)

| Group | Files | Action |
|---|---|---|
| **Dashboards** | `engine/dashboard.html` (48K active), `dashboard_LIVE_v1.html` (42K backup per CLAUDE.md #7), `dashboard_original_backup.html` (31K), `dashboard_signalboard_backup.html` (56K), `dashboard_with_alerts_backup.html` (41K) | 4 backups — move to `engine/archive/dashboards/` or delete, keep `dashboard.html` + `LIVE_v1` per rule |
| **Debug SPXU** | `engine/debug_spxu.py` (6.2K) + `debug_spxu2.py` (8.9K) ... `debug_spxu7.py` (3.0K) — 7 files | 7 iterations of same debug, none imported (`grep import debug_spxu` 0 hits) — consolidate to `engine/debug/` or delete |
| **Find symbols** | `engine/find_symbols.py` (1.6K), `engine/find_symbols2.py` (1.8K) | 2 variants, both unimported — merge |
| **IXIC checks** | `engine/check_ixic.py` (605B), `debug_ixic.py` (3.7K), `build_ixic_chart.py` (19K), `ixic_56reversal...html` (43K) | 4 IXIC paths, overlapping — consolidate |
| **Check helpers** | `engine/check_data_range.py`, `check_sso_ma500.py`, `check_tickers.py` (786B) | 3 check_*.py, all standalone, duplicate `load_env_config`/`get_influx_client` |

## 2. Orphaned Modules (0 imports)

- `engine/debug_ixic.py`, `debug_spxu*.py` (7), `find_symbols*.py` (2), `check_*.py` (3) — `grep -r "import.*debug"` 0 hits
- `engine/aso_investigate.py` (1.9K) — standalone, not in main pipeline (`detector.py` + `ingest.py` + `dashboard.html` per SETUP.md)
- `engine/reports/`, `engine/notes/` dirs — empty or ad-hoc, not referenced
- `engine/scan_results.txt` (0B) — dead artifact
- `engine/alerts.log` (1.3M) — log, not code, should be `.gitignore` + `logs/`
- `engine/backtest_data.json` (37K), `backtest_chart.html` (24K) — backtest artifacts, not in `archive/` nor `reports/`
- `engine/sma_alerts.html` — OLD system per CLAUDE.md (`sma_alert_engine.py` retired, Flask 5050) — should be in `engine/archive/old_system/`

## 3. Duplicated Utility Functions (inspect before move)

`grep -h "^def " engine/*.py | sort | uniq -c | sort -rn` — top duplicates:

| Duplicate | Count | Files |
|---|---|---|
| `def main():` | 5 | `detector.py`, `ingest.py`, `historical_scan.py`, `score_live.py`, `scorer.py` |
| `def load_env_config():` | 4 | `config.py`, `data_source.py`, `detector.py`, `ingest.py` |
| `def get_influx_client():` | 4 | same 4 |
| `def compute_all_smas(df):` | 4 | `detector.py`, `scorer.py`, `score_live.py`, `historical_scan.py` |
| `def get_system_status(client):` | 3 | `detector.py`, `scorer.py`, `score_live.py` |
| `def compute_sma`, `def cleanse_bars`, `def detect_precision_buy`, `def detect_optimized_buy` | 2 each | `detector.py` + `scorer.py` |

→ Single source of truth needed: `engine/utils/` or `engine/lib/` for shared `influx`, `sma`, `scoring`.

## 4. Dead Code Paths

- `engine/dashboard_original_backup.html` etc. — never served, `chart_server.py` serves `dashboard.html` only
- `engine/56_reversal_activity.html` (11K), `mockup_chart.html` (12K), `live_chart.html` (25K) — 3 chart variants, only `live_chart.html` is popup from dashboard
- `engine/backtest_spxu.py` (18K) — imports `backtest_data.json` but that file is not generated by `ingest.py` — dangling pipeline
- `engine/build_ixic_chart.py` — standalone, not in `refresh_calendar.py` flow

## 5. Misaligned Directory Boundaries

| Current | Should be | Why |
|---|---|---|
| `archive/refresh_calendar.py` (17K) | `engine/` or `scripts/` | Calendar build is code, not archive data; `archive/calendar/build_prices.py` already in `archive/calendar/` — split |
| `archive/calendar/index.html` (48K) | `engine/dashboard/` or `dashboard/` | Calendar HTML overlaps engine dashboards — 2 HTML roots |
| `archive/calendar/*.js` (`entries.js`, `opex_entries.js`, etc.) | `engine/calendar/` or `dashboard/` | JS for UI lives with HTML, not with archived JSONL (`_posts.json`) |
| `archive/*.json` (`_auto_state.json`, `_enrich.json`, etc.) | `archive/state/` | 4 state files at root, should be `archive/state/` |
| `reference/unfairmarket/` | `archive/reference/` or top `docs/` | Third-party case studies are reference, but currently under `reference/` not `archive/` — 2 archive roots |
| `engine/*.html` (7 files) + `archive/calendar/*.html` | `engine/dashboard/` | HTML dashboards scattered across 2 dirs — violates standard `src/` vs `public/` |

## 6. Misaligned Naming (vs MUSE_CODE.md / CLAUDE.md)

- `MUSE_CODE.md` not found — project uses `engine/CLAUDE.md` (SMA Alert System — Project Instructions) as standard. Naming is `CLAUDE.md` not `MUSE_CODE.md` — accept `CLAUDE.md` as canonical.
- `engine/` mixes Python, HTML, logs, JSON, BAT — standard is `engine/src/`, `engine/public/`, `engine/scripts/`
- `archive/` mixes data (`catalog.jsonl`, `media/`, `enumeration/`) + code (`refresh_calendar.py`) + UI (`calendar/index.html`)

## Summary Counts

- Redundant: 5 dashboards + 7 debug + 2 find + 3 check = 17 files
- Orphaned: 13 modules (7 debug + 2 find + 3 check + 1 aso_investigate + scan_results.txt)
- Duplicated defs: 5× main, 4× load_env_config/get_influx_client/compute_all_smas
- Misaligned: 8 path moves
- Dead: 5 HTML variants + 2 JSON artifacts + 1 log

Next: Phase 2 Fan-Out — parallel worktrees per directory (engine/dashboards, engine/debug, engine/utils, archive/calendar, reference).
